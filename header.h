#ifndef HEADER_H
#define HEADER_H
#include <SDL/SDL.h>
#include <SDL/SDL_image.h>
#include <SDL/SDL_mixer.h>
#include <stdio.h>
#include <stdlib.h>


// Enumeration for different states
enum GameState {
    MAIN_MENU,
    OPTIONS,
    CREDITS,
    LEVEL,
    EXIT_GAME,
};


#endif
